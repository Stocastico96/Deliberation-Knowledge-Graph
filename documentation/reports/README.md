# Reports

Historical reports, status notes, and runtime logs.

- `status/` contains project status and completion summaries.
- `logs/legacy/` contains logs that were previously tracked in the repository root.

